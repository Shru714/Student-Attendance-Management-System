# Database Configuration
# Update these values according to your phpMyAdmin/MySQL setup
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=attendance_system

# Server Configuration
PORT=3000
JWT_SECRET=your-secret-key-change-this-in-production

# Environment
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Update DB_PASSWORD with your MySQL root password
# 3. If using XAMPP/WAMP, DB_PASSWORD is usually empty
# 4. If using custom MySQL setup, use your actual password
